#!/usr/bin/env bash
# Run the tk test suite with summary output
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if [[ $# -gt 0 ]]; then
    exec "${SCRIPT_DIR}/bats/bin/bats" --pretty "$@"
else
    exec "${SCRIPT_DIR}/bats/bin/bats" --pretty "${SCRIPT_DIR}"/*.bats
fi
